const React = require('react');

const NotFound = React.createClass({

  render : function() {
    return (
      <div>
        <h1>Not here mate...</h1>
      </div>
    )
  }
});
module.exports = NotFound;